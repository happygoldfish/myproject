$kind: http-request
url: "{{baseUrl}}/react/users/{{userId}}/"
method: DELETE
scripts:
  - type: afterResponse
    code: |-
      pm.test('Status code is 200, 202, or 204', function () {
        pm.expect(pm.response.code).to.be.oneOf([200, 202, 204]);
      });
    language: text/javascript
order: 2000